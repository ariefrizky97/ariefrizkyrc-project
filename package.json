{
  "name": "stayloka-client",
  "private": true,
  "scripts": {
    "setup": "npm --prefix public install && npm --prefix cms install",
    "dev:public": "npm --prefix public run dev",
    "dev:cms": "npm --prefix cms run dev",
    "build": "npm --prefix public run build && npm --prefix cms run build",
    "lint": "npm --prefix public run lint && npm --prefix cms run lint"
  }
}
